<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.peterlangheinrich.tickama"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="16" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="de.peterlangheinrich.tickama.Home"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="de.peterlangheinrich.tickama.SimpleTicker"
            android:label="@string/title_activity_simple_ticker"
            android:parentActivityName="de.peterlangheinrich.tickama.Home" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="de.peterlangheinrich.tickama.Home" />
        </activity>
    </application>

</manifest>